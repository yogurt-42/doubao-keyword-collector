# 安全说明

## 报告问题

请通过仓库的私密安全报告功能提交漏洞。不要在公开 Issue 中粘贴 Cookie、账号目录、请求头、二维码、截图或完整日志。

## 部署边界

- 默认仅监听本机回环地址。
- 如需在局域网或公网访问，请先设置强随机 `DOUBAO_API_KEY`，并使用可信反向代理提供 HTTPS 和额外身份验证。
- 浏览器账号目录等同于登录凭据，应限制操作系统文件权限并做好本地备份。
- 不要把数据目录、`.env`、浏览器截图或调试跟踪提交到 Git。

