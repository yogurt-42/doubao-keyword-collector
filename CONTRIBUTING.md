# 参与贡献

欢迎提交问题、文档和代码修复。

提交前请确保：

1. 不包含任何真实账号的 Cookie、Token、二维码、截图或浏览器用户目录。
2. 不加入激活码、付费墙、设备指纹或远程授权逻辑。
3. 新接口默认保持本地优先，并补充相应测试。
4. 运行 `pytest` 和 `ruff check .`。

涉及豆包网页选择器或请求结构的修复，请只提交实现所需的最少信息，不要上传完整会话数据。

